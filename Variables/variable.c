#include <stdio.h>

int main(){
  int x;    //declaration
  x = 69;   //initialization

  int y = 420; // declaration + initialization
  
  int sum = x + y;
  printf("%d + %d = %d", x, y, sum);
  
  return 0;
}
